const express = require('express');
const mongoose = require('mongoose');
const { Schema } = mongoose;
const userModel = require('../models/User');

module.exports = {
    validateData : (uName, pwd, email) => {
        
    }
}
