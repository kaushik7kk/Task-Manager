import { createSlice } from "@reduxjs/toolkit";

export const formSlice = createSlice({
    name: 'forms',
    initialState : false,
    reducers: {
        toggleWindow: (state, action) => {
            
        }
    }
})